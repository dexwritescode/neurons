[package]
name = "tokenizers_sys"
version = "0.23.1"
edition = "2024"

[lib]
name = "tokenizers_sys"
crate-type = ["staticlib"]

[dependencies]
tokenizers = { version = "=0.23.1", default-features = false, features = ["onig"] }
